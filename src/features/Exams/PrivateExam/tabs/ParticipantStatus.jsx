import React from "react";
import { Typography } from "@mui/material";

function ParticipantStatus() {
  return (
    <Typography variant="body1">
      View the current statuses of all participants.
    </Typography>
  );
}

export default ParticipantStatus;
